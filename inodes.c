#include"inodes.h"

int is_directory(uint16 inode_mode)
{
	if(inode_mode >= ... && inode_mode < ...)
		return 1;
	return 0;
}


int is_file(uint16 inode_mode)
{
	if(inode_mode >= ... && inode_mode < ...)
		return 1;
	return 0;
}